@model B_R.Models.Equipamento

@{
    Layout = null;
var idArea = Session["areaid"];
}    

<!DOCTYPE HTML>

<html>
<head>

    @*<img src="~/Content/images/Logo-Default.png" height="100px" style="position:absolute;z-index:1;margin-top: 1%; margin-left: 5%;"/>*@
    @*<title>Brasil Refino</title>*@
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="~/Content/assets/css/main.css" />
</head>
<body>

    <header id="header">
        <div class="inner">
            @*<img src="~/Content/images/Logo-Default.png" class="logo" />*@
            <a href="@Url.Action("Index","Panel")" class="logo"><strong></strong> BRASIL REFINO</a>
            <p class="logo" style="margin-top:3%">Bem vindo, @Session["user"]</p>
            <nav id="nav">
                <a href="@Url.Action("Index","Area")">Área</a>
                <a href="#">solicitação</a>
                <a href="#">relatorio</a>
                <a href="#">plano de inspeção</a>
                <a href="#">novo</a>
                <a href="#">novo</a>
                <a href="@Url.Action("Logoff","Home")">Logoff</a>
                <a href="@Url.Action("Index","Equipamento",new { id = idArea})" class="  " style="color:#83d0a5">Voltar </a>
            </nav>
            <a href="#navPanel" class="navPanelToggle"><span class="fa fa-bars"></span></a>
        </div>
    </header>

    <!-- Banner -->
    <section id="banner">
        <div class="inner">
            <header>
                <h1>Editar Equipamento</h1>
            </header>

            <div class="flex text-center ">

                <form method="post" action="@Url.Action("Editar","Equipamento",new {idArea = idArea })" style="width:500px;margin-bottom:1%">


                    @*<input style="margin-bottom: 1%;" type="text" name="reg" id="reg" value="@Model.reg" placeholder="reg">*@
                    <input style="margin-bottom: 1%;margin-bottom: 1%;background: #555;border: solid 2px #555;" type="text" name="tag" id="tag" value="@Model.tag" placeholder="tag" readonly />
                    <select style="margin-bottom: 1%;" name="situacao" id="situacao">
                        <option value="">- situação -</option>
                        @if (Model.situacao == B_R.Models.situacao.ATIVO)
                        {
                            <option selected value="ATIVO">ATIVO</option>
                            <option value="FORA_DE_OPERACAO">FORA DE OPERACÃO</option>
                            <option value="EM_MONTAGEM">EM MONTAGEM</option>
                            <option value="DESATIVADA">DESATIVADA</option>
                        }
                        else if (Model.situacao == B_R.Models.situacao.FORA_DE_OPERACAO)
                        {
                            <option value="ATIVO">ATIVO</option>
                            <option selected value="FORA_DE_OPERACAO">FORA DE OPERACÃO</option>
                            <option value="EM_MONTAGEM">EM MONTAGEM</option>
                            <option value="DESATIVADA">DESATIVADA</option>
                        }
                        else if (Model.situacao == B_R.Models.situacao.EM_MONTAGEM)
                        {
                            <option value="ATIVO">ATIVO</option>
                            <option value="FORA_DE_OPERACAO">FORA DE OPERACÃO</option>
                            <option selected value="EM_MONTAGEM">EM MONTAGEM</option>
                            <option value="DESATIVADA">DESATIVADA</option>
                        }
                        else if (Model.situacao == B_R.Models.situacao.DESATIVADA)
                        {
                            <option value="ATIVO">ATIVO</option>
                            <option value="FORA_DE_OPERACAO">FORA DE OPERACÃO</option>
                            <option value="EM_MONTAGEM">EM MONTAGEM</option>
                            <option selected value="DESATIVADA">DESATIVADA</option>
                        }

                    </select>
                    <select style="margin-bottom: 1%;" name="status_Doc" id="status_Doc">
                        <option value="">- status do documento -</option>
                        @if (Model.status_Doc == B_R.Models.status_Doc.ATUALIZADO)
                        {
                            <option selected value="ATUALIZADO">ATUALIZADO</option>
                            <option value="PENDENTE">PENDENTE</option>
                        }
                        else
                        {
                            <option selected value="PENDENTE">PENDENTE</option>
                            <option value="ATUALIZADO">ATUALIZADO</option>
                        }
                    </select>
                    <input style="margin-bottom: 1%;margin-bottom: 1%;background: #555;border: solid 2px #555;" type="text" name="descricao" id="descricao" value="@Model.descricao" placeholder="descricao" readonly />
                    <input style="margin-bottom: 1%;margin-bottom: 1%;background: #555;border: solid 2px #555;" type="text" name="descricao_equipamento" id="descricao_equipamento" value="@Model.descricao_equipamento"  placeholder="descricao equipamento" readonly />
                    <button class="button" style="box-shadow:none">Editar</button><br />
                    <a class="button" href="@Url.Action("Index","Equipamento",new { id = idArea})" style="box-shadow:none">Voltar</a>
                </form>
            </div>

            <footer></footer>
        </div>
    </section>


    <!-- Scripts -->
    <script src="~/Content/assets/js/jquery.min.js"></script>
    <script src="~/Content/assets/js/skel.min.js"></script>
    <script src="~/Content/assets/js/util.js"></script>
    <script src="~/Content/assets/js/main.js"></script>

</body>
</html>